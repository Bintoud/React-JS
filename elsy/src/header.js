/*

import React, { Component } from 'react';
import logo from './logo.svg';
import '../public/css/style.css';
import App from '../src/App';   //chemin relatif du fichier que j'ai créer.
import Person from '../src/components/Person';
*/


// afin d'utiliser mon composant App je dois l'importer.
// je crée une balise ouvrante/ fermante qui contient le nom de mon composant (App) for use.
// ansi on crée une relation parent(header) enfant(App) 
//App est l'enfant car il est utiliser dans un autre composant.

/*
 class header extends Component {

    render() {
        return (
            <div className = "App">;
                <App></App>;  
            </div>
        );
    };
};

export default header; 
*/

// Pour exécuter le code ds la commande il faut aller dans le dossier ou se trouve le package.json
// et taper npm start.



